using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class schoolstuff2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
      // if (isGrounded && inputManager.playerIsmoving)
      // {
      //     PlayFootstepSound();
      //
      //
      //     curMoveInput = inputManager.moveDir * moveSpeed * Time.deltaTime;
      //     transform.forward = inputManager.moveDir;
      //
      //     rb.MovePosition(rb.position + curMoveInput);
      //     if (isGrounded && inputManager.playerIsmoving)
      //     {
      //         PlayFootstepSound();
      //         walkingEffect.Play();
      //
      //         Stashed changes
      //     }
      //     else
      //     {
      //         StopFootstepSound();
      //         Updated upstream
      //     }
      //
      //
      //     walkingEffect.Stop();
      // }
      // Stashed changes
    }
}
